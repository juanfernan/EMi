import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Portal Clientes y Ventas Movil V2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
